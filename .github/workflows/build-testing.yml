name: testing
on:
  push:
    branches:
      - testing
    paths-ignore:
      - "**.md"
  workflow_dispatch:

jobs:
  build-testing:
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    with:
      target: server
      variant: hci
      configuration: nvidia
      tag: testing
